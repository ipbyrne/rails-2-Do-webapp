<%= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>
<%= f.text_field :content, placeholder: "New Todo Item" %>
  <%= f.submit label: "Create Todo Item", class: "todo_item_btn"%>
<% end %>